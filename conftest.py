"""Present so that pytest puts the repository root on sys.path.

That is the only reason this file exists: it means `python3 -m pytest` works
from a fresh clone with nothing installed except pytest.
"""
