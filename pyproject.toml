[project]
name = "evalharness"
version = "0.1.0"
description = "A small refuse-or-cite evaluation harness over fixture LLM outputs. Standard library only."
readme = "README.md"
license = { file = "LICENSE" }
authors = [{ name = "Nikhil Sood" }]
requires-python = ">=3.9"
dependencies = []

[project.optional-dependencies]
dev = ["pytest>=7.0"]

[tool.pytest.ini_options]
testpaths = ["tests"]
